describe('Circuit tests', () => {
    it('should allow an object to have a default active state', () => {});
    it('should let a lever control a door', () => {});
    it('should let a lever only set another objects active state if it also has the same active state', () => {});
});
