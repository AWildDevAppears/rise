describe('Circuit tests', () => {
    xit('should allow an object to have a default active state', () => {});

    xit('should let a lever control a door', () => {});

    xit('should let a lever only set another objects active state if it also has the same active state', () => {});
});
